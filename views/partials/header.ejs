<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YelpCamp</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/main.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">YelpCamp</a>
        </div>        
        <div>
            <ul class="nav navbar-nav navbar-right collapse navbar-collapse">
                <% if(!currentUser){ %>
                    <li><a class="btn btn-default ml-2" href="/login">Login</a></li>
                    <li><a class="btn btn-default ml-2" href="/register">Sign Up</a></li>
                <% }else{ %>
                    <li><a href="#"></a> Sign In As <%= currentUser.username %></li>
                    <li><a class="btn btn-default ml-2" href="/logout">Logout</a></li>
                <% } %>
            </ul>
        </div>
    </nav>
</div>
<div class="container">
    <% if(error && error.length > 0){ %>
        <div class="alert alert-danger">
            <%= error %>
        </div>
    <% } %>
        
    <% if(success && success.length > 0){ %>
        <div class="alert alert-success">
            <h1><%= success %></h1>
        </div>
    <% } %>
</div>

    
